= simple_form_for @subject, html: { class: 'form-vertical'} do |f|
  = f.input :title
  = f.input :explain, :as => :text
  = f.button :submit, class: 'btn-primary' 
  = link_to t('cancel'), subjects_path, class: 'btn'

